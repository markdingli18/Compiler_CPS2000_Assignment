LEXER
 
function arrow
check function call?
metion can't have identifiers same name as type

-------------------------------------------------------------------------------------------------
PARSER

function arrow

-------------------------------------------------------------------------------------------------
TASK 3

-------------------------------------------------------------------------------------------------
TASK 4



-------------------------------------------------------------------------------------------------


let x: int = 5 * 20 + 5;
let y: float = 3.14;
let z: bool = true;
let y: bool = false;
let w: colour = #FF0000;

__print(x);
__delay(1000);
__pixel(0, 0, 255);
__pixelr(0, 0, 255, 300, 0);
__width(x);
__height(y);
__read(x,y);
__randi(x);

if (x < 50) {
  __print("x is less than 50");
} else {
  __print("x is greater than or equal to 50");
}

while(x>10){
    __print(x);
}

for (let x: int = 0; x < 10; x = x + 1) {
  __print(x);
}

fun ret(x: int, y: int) -> int {
  return x + y;
}

let result: int = add(5, 10);
__print(result);